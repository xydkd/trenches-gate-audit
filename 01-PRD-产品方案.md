# 产品方案 v1.0.0 — 实时发现与证据观察

状态：修复后的观察发布包；实际Grok启用与首跑未由仓库证明。默认每日08:00 Asia/Hong_Kong，过去24h。

## 1. 产品目标

10分钟内读懂谁提到了什么新对象、证据是否齐全、有哪些未知或规则冲突。允许没有对象，不要求每日有标的。不以盈亏、后见涨幅或门通过数作为成功标准。

Grok实时检索X用于发现、人脉一跳、原帖/线程、官方事件、传播温度和中文观察。量化数据需独立可访问来源与方法说明；X实时性不能证明市值、fo、持仓或可卖性实时有效。

## 2. 范围

本版本仅OBSERVATION。保留A/B/C/D分类，但A/B仅能产生观察状态，C/D评分关闭。所有类型均不输出份数、仓位比例、加仓、退出、目标价或自动交易。OBSERVE不是已通过完整交易安全审查。

不引入热搜冷启动、随机ticker、收费群/事后倍数发现、D二次监控冷启动。不扩展0–20分钟PVP或持续监控。不把每日新闻任务合并进来。

## 3. 发现与来源

唯一种子清单为 [seeds.json](config/seeds.json)。先扫w3，再有限扫描w2，实际一跳最多8人，之后才能查对象；grimsmarket仅实际一跳，SkyAAmen仅Circle Arc限定场景。权重只决定阅读顺序。利益关系必须当期核验，历史标签只作待核线索。

六语法仍为PERMISSION、VACUUM、TANDEM、MECHANISM、LOTTERY、LAG，允许多标签。没有语法不入表。混合LOTTERY不能通过A/B/D兜底。P2传播拥挤先于P0/P1；中文未完整覆盖不得升P0/P1，也不声称全网无信念长文。

项目、链、token分开；名称不是资产主键。token量化证据必须绑定经核验的chain_id+CA，同名Arc对象不拼接。

## 4. 输出

七节：运行健康与天气、最多8行新对象、种子换题、FOMO证据、中文滞后观察、证据卡、下次检查与免责。末尾完整JSON，正文必须与其一致。检查最多3项，有几项写几项。

状态：UNKNOWN（不足）、FAIL（已知条件不符）、DISABLED（类型关闭）、OBSERVE（当前A/B观察条件齐全）。run_status与对象状态分开：FAILED不是quiet；只有成功完成规定扫描的真实空窗才quiet。

字段、阈值与严格顺序见 [统一规则](docs/RULES.md)。单个网页打不开无需编故事补数，也不要求为了避免UNKNOWN而加数据源或绕登录。

## 5. 接受标准

来源路径、语法证据、身份、观测时间和缺失字段可追溯；真实工具故障能看到；无仓位动作；C不可绕行；没有假复核。原五个案例用于检查有限事实下的推理，缺数据的PONS/CASHCAT不强制通过。

本地测试、Grok实际首跑和后续质量观察分别验收。后续如需规则评分、持久化、真实T0/T1或组合研究，建立新版本重新设计与测试，不在此提示词中临时打开。
