# 战壕发现与交易门系统 — 产品方案（送审稿）

- 版本：v0.9（审计前冻结）
- 日期：2026-09-05
- 状态：**未上线 v2。** 现网仅有发现层早报任务 `trenches-discovery-am`（每日 08:00 Asia/Hong_Kong）。本方案通过审计前，不得改现网 prompt、不得加自动交易。
- 文档读者：产品负责人、风控/审计、实现同学
- 语言：中文。专有名词保留英文。

---

## 0. 一句话

每天用固定的人、固定的叙事语法、固定的量化门槛，产出一份「发现早报 + 交易门打分表」。**系统不下单、不跟单、不喊买。** 人看门，人决策。

---

## 1. 要解决的问题

复盘 2026 年 7–9 月 Robinhood Chain（CASHCAT / PONS / $AI）后，用户侧有三块缺口：

1. **信息差**：英文信念定价比中文 SOP 早 4–6 周。关键词每次都变，跟 ticker 会永远迟到。
2. **看得到买不下手**：即使第一时间读到论文（如 `$ZZZ`），没有「做不做、做哪类、多大、何时认错」的门。
3. **战场已有量化，我们没用**：中文圈用 GMGN / FOMO 二次监控（含 fo 量、具名钱包、鲸鱼、5 分钟复核）。我们之前只把 fo>20% 当「发现结束」，没有二段交易规则。

本产品把这三块收成一个定时任务，而不是再做一个打狗终端。

---

## 2. 用户与场景

| 角色 | 场景 | 成功长什么样 |
|---|---|---|
| 主用户（你） | 每天 08:00 打开早报，10 分钟内知道「有没有新对象、门过没过」 | 空表也是成功；不是每天必须有标的 |
| 审计/同事 | 读本方案，判断会不会变成喊单机或关键词扫描器 | 规则可证伪、可回测、不下单 |
| 未来实现 | 按实现方案改 prompt / 加二次采集 | 不发明新语法 |

非用户：跟单机器人、收费群、返佣漏斗。

---

## 3. 定位与非目标

**定位**：观察扫描 + 交易门打分。情报产品，不是交易执行产品。

**做：**

- 从人出发发现新对象（种子 → 一跳 → 对象）
- 用六句叙事语法分类
- 检测 FOMO 群（发现结束灯）
- 测中文滞后
- 对进入视野的对象填量化记分卡，给出 A/B/C/D 交易门
- 输出证伪句和下 24h 检查

**明确不做：**

- 自动买卖、跟单、刮刀下单
- 以关键词/热搜/GMGN 前十作为发现入口
- 把 fo>20% 单独当成买入信号
- 0–20 分钟内盘 PVP
- 通用新闻日报（现网另有「每日新闻」，本产品不合并）
- 投资建议、目标价、仓位金额（只给「份数」相对单位）

**合规句（每份输出必须出现）**：观察扫描，不构成投资建议。交易门是规则打分，不是下单指令。

---

## 4. 产品原则（审计时用这 8 条打）

1. **人先于词。** 发现入口是加权种子账号，不是 ticker 列表。
2. **语法先于故事。** 没有 PERMISSION / VACUUM / TANDEM / MECHANISM / LOTTERY / LAG 之一，对象丢弃。
3. **数字填不出 = 否。** 交易门不允许用作文补空。
4. **斜率与水平分开。** fo 急升是拥挤灯；洗盘后 fo 稳住才可能是 D 类门槛。
5. **发现层与交易层分列。** 发现可以是空表；交易门只对已入表对象打分。
6. **利益冲突显式标注。** 返佣、收费群、项目方、持仓口吻必须写在「首次被谁看到」旁。
7. **C 类默认关闭。** 新闻皮/身份彩票默认不做。打开需产品负责人书面确认。
8. **系统永不下单。** 任何把输出接到 Debot/GMGN 跟单的改动，视为范围外新项目。

---

## 5. 信息架构：一份早报的七块

每日一份，中文，短。对象表最多 8 行。空表合法。

```
1. 天气：discovery / confirmation / extreme-FOMO / quiet
2. 新对象表（发现层）
3. weight-3/2 换题
4. FOMO 群面板（灯，不是买点）
5. 中文滞后
6. 交易门记分卡（本方案新增，只对第 2 节对象 + 二次监控候选）
7. 下 24h 可证伪检查 + 免责
```

第 6 块是 v2 相对现网唯一产品增量。第 2 块规则不变。

---

## 6. 发现层（现网已有，审计时核对是否被改坏）

### 6.1 种子（角色，不是神谕）

权重管「读的优先」，不管「能不能跟」。

| 档 | 账号 | 角色 | 利益 |
|---|---|---|---|
| w3 分析 | AvgJoesCrypto | 费率表，TANDEM 数据腿 | 低喷洒 |
| w3 分析 | PhilOnChain | 可证伪清单 | — |
| w3 换题 | theunipcs | VACUUM+TANDEM 过程帖 | FOMO 返佣 |
| w3 原型 | neodot | 真空现场「几小时变第一」 | 与 Pons 对齐 |
| w2 雷达 | The__Solstice, 0xAvast, grimsmarket | 仅新对象或新语法 | 返佣/持仓 |
| w1 温度 | Kaleo, IcedKnife, lookonchain, Vlad, 官号 | 从不单独当发现 | 神话/彩票/盖章 |
| w0 发现 / w3 滞后 | jiamigou, tmel0211, 吴说/PANews | 中文温度计 | SOP/媒体 |
| 可选 w2 一跳 | SkyAAmen | **仅 Circle Arc 战壕**，RH 发现不加 | 收费群 |
| 永不进种子 | dudunode 类 | 事后倍数 + TG 导流 | 招生漏斗 |

维护规则：增删种子必须附「哪次复盘、哪条帖、权重理由」。禁止因某天喊中而升 w3。

### 6.2 扫描顺序（禁止颠倒）

1. `from:种子` 不带关键词  
2. 一跳：他们引用/转/回的作者  
3. **只有种子或一跳点名了新对象**，才搜该对象  
4. 禁止用 ticker 列表冷启动  

### 6.3 六句语法 → P 级

| 语法 | 含义 | 典型 |
|---|---|---|
| PERMISSION | 官方改口/关注/上架 | Vlad 关注 CASHCAT |
| VACUUM | 别人死了它几小时变第一 | Noxa → PONS |
| TANDEM | 数据号 + 信念号 72h 内对上 | AvgJoes 费率 + Unipcs 上仓 |
| MECHANISM | 链上可核原语，不是口号 | 股票计价+税进 RWA；托管金库不算 |
| LOTTERY | 身份/截图彩票 | Lookonchain 图 |
| LAG | 英文已定价、中文无信念文 | Haotian 晚 4–6 周 |

- 无语法 → 丢  
- P0 = PERMISSION\|VACUUM\|TANDEM 且中文无信念文  
- P1 = MECHANISM\|LOTTERY 且中文只有快讯  
- P2 = 英文已极端 FOMO，或 FOMO 群 2 族同向  

口号 MECHANISM 必须打开产品核验（TryArcFinance：treasury + 数据库 = 否）。

### 6.4 FOMO 群（发现结束灯）

特征族（稳定，ticker 变）：换皮、模板 maxi、返佣 bio、机器人克隆、引用堆、事后先知、GMGN/FOMO 堆车。

24h 内同一对象 ≥2 族 → 标 P2，停止当发现。

---

## 7. 交易层（v2 新增）

发现回答「是不是新对象」。交易门回答「做不做、哪一类、几份、何时认错」。

### 7.1 四类单

| 类型 | 语法/场景 | 默认 |
|---|---|---|
| A 结构 | VACUUM / TANDEM / 核过的 MECHANISM | 开 |
| B 盖章 | PERMISSION 且 72h 内、公域未走完 | 开 |
| C 事件 | LOTTERY / 新闻皮 | **关** |
| D 二段 | 外盘已洗，fo 稳住，有「谁在买」 | 开 |

同一对象只归一类。不能因为故事酷把 C 改成 A。

### 7.2 量化字典（交易门必填）

数字 15 分钟内要能从公开页读到。读不到写 `未填`，未填在门槛里 = 否。

**硬否（一条即否）**

- 内盘（D 不做内盘；A 仅真空刚毕业可例外）  
- 不能卖出 / 貔貅  
- 捆绑% 或内部% > 20%  
- dev 卖出 > 0（C/D）  
- 投票机器人同 CA、跟单猎杀盘  
- fo 24h 升幅 > 10 个百分点（停新开）  
- fo 水平 > 40%（D 否）  
- 论文号已在当主推且市值进入传播带（C/D 视为已在传播层）

**D 主区（须同时满足）**

- 外盘市值 **$30 万–$300 万**  
- fo **15–25%** 且斜率不急升  
- 持仓人数在回撤中不掉  
- 换手 24h/MC 在 **0.3–2x**  
- **具名钱包 ≥2 同向，或鲸鱼 24h 净流入 > 0**（二选一）  
- 信号后 **5 分钟复核** 仍成立  

**A/B 另加**

- A：板上有第三方费率/份额数字  
- B：盖章未满 48h，市值未进 $5000 万  
- A/B 开仓可以暂时无 fo；**fo>25% 且斜率向上时禁止加仓**

### 7.3 仓位（相对「份」，总资金切 10 份）

| 类型 | 首仓 | 加仓 | 出 |
|---|---|---|---|
| A | 2 | 第三方证费率后再 1–2 | 真空填上且 FOMO 灯亮，或份额掉回 |
| B | 1–2 | 官方第二下且市值 <$5000 万 | 盖章证伪或进 $5000 万+ 且灯亮 |
| C | 0（默认关）；若打开最多 0.5–1 且不加 | — | 12h / 证伪 / $1000 万 / 灯，先到先出 |
| D | 1（fo 20–25% 且洗完不掉可 1.5） | 不加 | 市值穿 $300 万 / fo 穿 40% / dev 卖 / 机器人 / 人数掉头 |

系统只输出「类型 + 份数 + 证伪句」，不输出应买金额、杠杆、交易所。

### 7.4 二次监控的产品角色

GMGN / FOMO / Stalkchain / windvane / Hoodwatch 是 **D 的原料厂和 A/B 的否决屏**，不是发现入口。

禁止：

- GMGN 热搜第一直接进对象表  
- FOMO Top100 全量跟单  
- 0–20 分钟战壕当早报主线  
- 把返佣帖里的「工具总结」当官方数据源（字段可抄，链接当广告）

允许：对已经因语法入表的对象，补 fo%、持仓人数、捆绑、具名同向、鲸鱼净流入。可选：从 Stalkchain 具名买入 + windvane fo 在 D 带内的交集，生成 **最多 3 条 D 候选**，每条必须跑完整记分卡，标 `来源:二次监控（非发现）`。

---

## 8. 回归用例（审计必须用这 5 个打产品）

| 对象 | 当时窗口 | 发现层期望 | 交易门期望 |
|---|---|---|---|
| CASHCAT 7/2–7/8 | Solstice 论文 → Vlad 关注 | P0/P1 盖章前 | B 可（盖章后、App 未上前）；之后灯亮减 |
| PONS 7/15 | Noxa 死，neo 说几小时第一 | P0 VACUUM | A 可；后来 fo 升高应减不应追 |
| TryArcFinance 9/5 | 项目方宣言 | 丢（机制核失败+撞名） | 否 |
| ZZZ 9/5 论文时刻 | 新闻皮 + 未核身份 + 随后机器人 | LOTTERY，不当发现 | C 关；D 市值带否 → 空仓 |
| DuDu Marscoin 788x | 公域无当时 call | 不进种子 | 不因事后倍数开 D |

若实现改完后这 5 条有一条翻成「建议买入」，审计不通过。

---

## 9. 成功标准（上线 14 天）

**过程指标（先看这些，不看盈亏）**

- 空表天数被接受，不靠关键词灌行  
- 每条入表对象都有：首次被谁看到（含权重+利益）、语法、链接、P 级  
- 交易门每条有：类型、市值、fo%/Δ、未填字段列表、门（可/否）、证伪句  
- 误把 Circle Arc 与 TryArcFinance `$ARC` 写成同一对象 = 事故  
- C 类默认无输出  

**结果指标（不当上线门槛）**

- 是否在中文 SOP 之前标出至少 1 个后来被证实的 P0 语法（真空/盖章）  
- D 类若开过，是否遵守市值带与 5 分钟复核（违规即失败）  
- 不评价「如果当时买了能赚多少」——产品成功 ≠ 模拟盘赚钱  

---

## 10. 风险

| 风险 | 等级 | 缓解 |
|---|---|---|
| 早报被当成喊单 | 高 | 每份免责；只出份数与否；C 默认关 |
| 种子利益冲突 | 高 | 表内强制写利益 |
| fo>20% 公开后失效 | 高 | 必须叠加「谁在买」+斜率；Kevin 已自陈失效 |
| 第三方页改版/登录墙 | 中 | 未填=否，不编数字 |
| X 单次搜索条数少 | 中 | 按人切片，不补关键词网 |
| 跟单猎杀盘 | 中 | 禁止跟单；无名聪明钱不升门 |
| 合规：扫第三方站点 | 中 | 只读公开页；不存用户资金；不绕过登录墙 |
| 范围膨胀成交易 bot | 高 | 非目标写死；接 Debot 需新立项 |

---

## 11. 里程碑

| 阶段 | 内容 | 上线条件 |
|---|---|---|
| v0 现网 | 发现层 08:00 早报 | 已运行 |
| **v1 本方案** | 早报加交易门；C 关；fo 必填 | **本审计通过** |
| v2 | 二次监控最多 3 条 D 候选 + 5 分钟复核字段 | v1 跑满 7 天无事故 |
| v3 | 独立采集服务/API key（fomoapi） | 另立项目，不在本次 |

本次送审范围 = **v1 文档 + 提示词 v2 草稿**。不改现网，直到审计签字。
